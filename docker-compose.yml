services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: stokpro
      POSTGRES_PASSWORD: stokpro
      POSTGRES_DB: stokpro
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
